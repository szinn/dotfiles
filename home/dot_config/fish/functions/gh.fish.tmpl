function gh --description="Wrap github gh"
    if test -d .git
        {{ lookPath "gh" }} $argv
    else if test -d .jj
        GIT_DIR=$PWD/.jj/repo/store/git {{ lookPath "gh" }} $argv
    else
        {{ lookPath "gh" }} $argv
    end
end
