auto_sync = true
ctrl_n_shortcuts = true
filter_mode = "workspace"
search_mode = "daemon-fuzzy"
{{- if .homeMachine }}
sync_address = "https://atuin.zinn.ca"
{{- end }}
update_check = false
workspaces = true
history_filter = [
  "^/opt/homebrew/bin/jj",
  "^cd",
  "^chezmoi",
  "^cm",
  "^gst$",
  "^ll",
  "^ls",
  "^x$",
]

[sync]
records = true

[daemon]
enabled = true
autostart = true
